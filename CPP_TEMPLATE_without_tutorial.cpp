#include <bits/stdc++.h>        
using namespace std;
using ll=long long int;
#define dbg(...)
#define db(...)
#ifdef DBG
#include "debug.h"  // https://github.com/amit-nits/justforsharing/blob/master/debug.h
#endif
ll n,m,p,q,k,u,v,cnt = 0,ans = -1,mod=1e9+7,mx=1e5+1,T = 1;
vector<ll> a(mx,-1);

void solution(){

}
void preprocess(){
      if(!T) cin>>T;
}
int main(){ 
      ios_base::sync_with_stdio(0); 
      cout.tie(0);
      cin.tie(0);
      cerr.tie(0);
      preprocess();
      while(T--) 
            solution();
      return 0;
}
